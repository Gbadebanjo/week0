// 1. Create a function that takes an array containing only numbers and return the first e

function getFirstValue(arr) {
  return arr[0];
}
console.log(getFirstValue([1, 2, 3]));
console.log(getFirstValue([80, 5, 100]));
